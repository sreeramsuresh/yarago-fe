# Docker environment configuration
# This file is used when building the frontend for Docker deployment
# Browser makes requests from host machine, so we use localhost with the exposed port
VITE_API_BASE_URL=http://localhost:9090
VITE_WS_URL=ws://localhost:9090/ws/notifications
